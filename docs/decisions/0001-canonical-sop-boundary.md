# ADR 0001: Canonical SOP boundary

**Status:** accepted

Application behavior depends on the provider-neutral canonical SOP contract. Provider payloads remain immutable raw artifacts. Human-reviewed canonical JSON supplies version diffs, readers, chunks, retrieval evidence, and citations. This prevents an OCR/provider schema change from rewriting downstream application logic.
